#==========================================================================
#   Validation common fields rules
#==========================================================================
validation.id.null = Field 'id' should be absent in request
validation.id.not_null = Field 'id' should be present in request
validation.genre.not_null = Field 'genre' should be present in request
validation.language.not_null = Field 'language' should be present in request
validation.balance.null = Field 'balance' should be absent in request
validation.status.null = Field 'status' should be absent in request
validation.status.not_null = Field 'status' should be present in request
validation.role.null = Field 'role' should be absent in request
validation.role.not_null = Field 'role' should be present in request
validation.publicationInfos.not_null = Field 'publicationInfos should be present in request
validation.subscriptions.null = Field 'subscriptions' should be absent in request

#==========================================================================
#   Validation User rules
#==========================================================================
validation.user.first_name = Must starts with a capital letter followed by 1 to 49 lowercase Latin letters
validation.user.last_name = Must starts with a capital letter followed by 1 to 49 lowercase Latin letters
validation.user.email = Must be a valid email address
validation.user.password = Must contains at least one digit,lowercase and uppercase Latin character, special symbol and a length from 8 to 32
validation.user.emailAlreadyExists = User with this email is already exists

#==========================================================================
#   Validation Publication rules
#==========================================================================
validation.publication.title = Must contains at least 2 symbols, max - 100 symbols
validation.publication.description = Must contains at least 10 symbols, max - 255 symbols
validation.publication.price_per_month = Must be a positive number